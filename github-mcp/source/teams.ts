// AUTO-GENERATED by scripts/generate.ts — DO NOT EDIT
import { z } from "zod";
import { githubRequest } from "../client.js";

export const teamsTools = [
  {
    name: "github_teams_list",
    description: "List teams",
    inputSchema: z.object({
      org: z.string().describe("org"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      team_type: z.enum(["all", "enterprise", "organization"]).optional().describe("Filter team results by their type. For more information, see \"[What kind of team should I use?](https://docs.github.com/enterprise-cloud@latest/admin/concepts/enterprise-fundamentals/teams-in-an-enterprise#what-kind-of-team-should-i-use)\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/teams`, undefined, { per_page: args.per_page, page: args.page, team_type: args.team_type });
    },
  },
  {
    name: "github_teams_create",
    description: "Create a team",
    inputSchema: z.object({
      org: z.string().describe("org"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/orgs/${args.org}/teams`, args.body, undefined);
    },
  },
  {
    name: "github_teams_get_by_name",
    description: "Get a team by name",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/teams/${args.team_slug}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_update_in_org",
    description: "Update a team",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/orgs/${args.org}/teams/${args.team_slug}`, args.body, undefined);
    },
  },
  {
    name: "github_teams_delete_in_org",
    description: "Delete a team",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/orgs/${args.org}/teams/${args.team_slug}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_list_pending_invitations_in_org",
    description: "List pending team invitations",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/teams/${args.team_slug}/invitations`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_teams_list_members_in_org",
    description: "List team members",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      role: z.enum(["member", "maintainer", "all"]).optional().describe("Filters members returned by their role in the team."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/teams/${args.team_slug}/members`, undefined, { role: args.role, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_teams_get_membership_for_user_in_org",
    description: "Get team membership for a user",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/teams/${args.team_slug}/memberships/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_add_or_update_membership_for_user_in_org",
    description: "Add or update team membership for a user",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      username: z.string().describe("username"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/orgs/${args.org}/teams/${args.team_slug}/memberships/${args.username}`, args.body, undefined);
    },
  },
  {
    name: "github_teams_remove_membership_for_user_in_org",
    description: "Remove team membership for a user",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/orgs/${args.org}/teams/${args.team_slug}/memberships/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_list_repos_in_org",
    description: "List team repositories",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/teams/${args.team_slug}/repos`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_teams_check_permissions_for_repo_in_org",
    description: "Check team permissions for a repository",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/teams/${args.team_slug}/repos/${args.owner}/${args.repo}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_add_or_update_repo_permissions_in_org",
    description: "Add or update team repository permissions",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/orgs/${args.org}/teams/${args.team_slug}/repos/${args.owner}/${args.repo}`, args.body, undefined);
    },
  },
  {
    name: "github_teams_remove_repo_in_org",
    description: "Remove a repository from a team",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/orgs/${args.org}/teams/${args.team_slug}/repos/${args.owner}/${args.repo}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_list_child_in_org",
    description: "List child teams",
    inputSchema: z.object({
      org: z.string().describe("org"),
      team_slug: z.string().describe("team_slug"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/teams/${args.team_slug}/teams`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_teams_get_legacy",
    description: "Get a team (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/teams/${args.team_id}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_update_legacy",
    description: "Update a team (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/teams/${args.team_id}`, args.body, undefined);
    },
  },
  {
    name: "github_teams_delete_legacy",
    description: "Delete a team (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/teams/${args.team_id}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_list_pending_invitations_legacy",
    description: "List pending team invitations (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/teams/${args.team_id}/invitations`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_teams_list_members_legacy",
    description: "List team members (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      role: z.enum(["member", "maintainer", "all"]).optional().describe("Filters members returned by their role in the team."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/teams/${args.team_id}/members`, undefined, { role: args.role, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_teams_get_member_legacy",
    description: "Get team member (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/teams/${args.team_id}/members/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_add_member_legacy",
    description: "Add team member (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/teams/${args.team_id}/members/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_remove_member_legacy",
    description: "Remove team member (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/teams/${args.team_id}/members/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_get_membership_for_user_legacy",
    description: "Get team membership for a user (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/teams/${args.team_id}/memberships/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_add_or_update_membership_for_user_legacy",
    description: "Add or update team membership for a user (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      username: z.string().describe("username"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/teams/${args.team_id}/memberships/${args.username}`, args.body, undefined);
    },
  },
  {
    name: "github_teams_remove_membership_for_user_legacy",
    description: "Remove team membership for a user (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/teams/${args.team_id}/memberships/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_list_repos_legacy",
    description: "List team repositories (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/teams/${args.team_id}/repos`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_teams_check_permissions_for_repo_legacy",
    description: "Check team permissions for a repository (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/teams/${args.team_id}/repos/${args.owner}/${args.repo}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_add_or_update_repo_permissions_legacy",
    description: "Add or update team repository permissions (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/teams/${args.team_id}/repos/${args.owner}/${args.repo}`, args.body, undefined);
    },
  },
  {
    name: "github_teams_remove_repo_legacy",
    description: "Remove a repository from a team (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/teams/${args.team_id}/repos/${args.owner}/${args.repo}`, undefined, undefined);
    },
  },
  {
    name: "github_teams_list_child_legacy",
    description: "List child teams (Legacy)",
    inputSchema: z.object({
      team_id: z.string().describe("team_id"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/teams/${args.team_id}/teams`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_teams_list_for_authenticated_user",
    description: "List teams for the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/teams`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
];
