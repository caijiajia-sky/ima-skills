// AUTO-GENERATED by scripts/generate.ts — DO NOT EDIT
import { z } from "zod";
import { githubRequest } from "../client.js";

export const usersTools = [
  {
    name: "github_users_get_authenticated",
    description: "Get the authenticated user",
    inputSchema: z.object({}),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user`, undefined, undefined);
    },
  },
  {
    name: "github_users_update_authenticated",
    description: "Update the authenticated user",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/user`, args.body, undefined);
    },
  },
  {
    name: "github_users_list_blocked_by_authenticated_user",
    description: "List users blocked by the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/blocks`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_check_blocked",
    description: "Check if a user is blocked by the authenticated user",
    inputSchema: z.object({
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/blocks/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_users_block",
    description: "Block a user",
    inputSchema: z.object({
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/user/blocks/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_users_unblock",
    description: "Unblock a user",
    inputSchema: z.object({
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/blocks/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_users_set_primary_email_visibility_for_authenticated_user",
    description: "Set primary email visibility for the authenticated user",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/user/email/visibility`, args.body, undefined);
    },
  },
  {
    name: "github_users_list_emails_for_authenticated_user",
    description: "List email addresses for the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/emails`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_add_email_for_authenticated_user",
    description: "Add an email address for the authenticated user",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/user/emails`, args.body, undefined);
    },
  },
  {
    name: "github_users_delete_email_for_authenticated_user",
    description: "Delete an email address for the authenticated user",
    inputSchema: z.object({}),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/emails`, undefined, undefined);
    },
  },
  {
    name: "github_users_list_followers_for_authenticated_user",
    description: "List followers of the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/followers`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_list_followed_by_authenticated_user",
    description: "List the people the authenticated user follows",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/following`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_check_person_is_followed_by_authenticated",
    description: "Check if a person is followed by the authenticated user",
    inputSchema: z.object({
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/following/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_users_follow",
    description: "Follow a user",
    inputSchema: z.object({
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/user/following/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_users_unfollow",
    description: "Unfollow a user",
    inputSchema: z.object({
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/following/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_users_list_gpg_keys_for_authenticated_user",
    description: "List GPG keys for the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/gpg_keys`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_create_gpg_key_for_authenticated_user",
    description: "Create a GPG key for the authenticated user",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/user/gpg_keys`, args.body, undefined);
    },
  },
  {
    name: "github_users_get_gpg_key_for_authenticated_user",
    description: "Get a GPG key for the authenticated user",
    inputSchema: z.object({
      gpg_key_id: z.string().describe("gpg_key_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/gpg_keys/${args.gpg_key_id}`, undefined, undefined);
    },
  },
  {
    name: "github_users_delete_gpg_key_for_authenticated_user",
    description: "Delete a GPG key for the authenticated user",
    inputSchema: z.object({
      gpg_key_id: z.string().describe("gpg_key_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/gpg_keys/${args.gpg_key_id}`, undefined, undefined);
    },
  },
  {
    name: "github_users_list_public_ssh_keys_for_authenticated_user",
    description: "List public SSH keys for the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/keys`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_create_public_ssh_key_for_authenticated_user",
    description: "Create a public SSH key for the authenticated user",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/user/keys`, args.body, undefined);
    },
  },
  {
    name: "github_users_get_public_ssh_key_for_authenticated_user",
    description: "Get a public SSH key for the authenticated user",
    inputSchema: z.object({
      key_id: z.string().describe("key_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/keys/${args.key_id}`, undefined, undefined);
    },
  },
  {
    name: "github_users_delete_public_ssh_key_for_authenticated_user",
    description: "Delete a public SSH key for the authenticated user",
    inputSchema: z.object({
      key_id: z.string().describe("key_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/keys/${args.key_id}`, undefined, undefined);
    },
  },
  {
    name: "github_users_list_public_emails_for_authenticated_user",
    description: "List public email addresses for the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/public_emails`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_list_social_accounts_for_authenticated_user",
    description: "List social accounts for the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/social_accounts`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_add_social_account_for_authenticated_user",
    description: "Add social accounts for the authenticated user",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/user/social_accounts`, args.body, undefined);
    },
  },
  {
    name: "github_users_delete_social_account_for_authenticated_user",
    description: "Delete social accounts for the authenticated user",
    inputSchema: z.object({}),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/social_accounts`, undefined, undefined);
    },
  },
  {
    name: "github_users_list_ssh_signing_keys_for_authenticated_user",
    description: "List SSH signing keys for the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/ssh_signing_keys`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_create_ssh_signing_key_for_authenticated_user",
    description: "Create a SSH signing key for the authenticated user",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/user/ssh_signing_keys`, args.body, undefined);
    },
  },
  {
    name: "github_users_get_ssh_signing_key_for_authenticated_user",
    description: "Get an SSH signing key for the authenticated user",
    inputSchema: z.object({
      ssh_signing_key_id: z.string().describe("ssh_signing_key_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/ssh_signing_keys/${args.ssh_signing_key_id}`, undefined, undefined);
    },
  },
  {
    name: "github_users_delete_ssh_signing_key_for_authenticated_user",
    description: "Delete an SSH signing key for the authenticated user",
    inputSchema: z.object({
      ssh_signing_key_id: z.string().describe("ssh_signing_key_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/ssh_signing_keys/${args.ssh_signing_key_id}`, undefined, undefined);
    },
  },
  {
    name: "github_users_get_by_id",
    description: "Get a user using their ID",
    inputSchema: z.object({
      account_id: z.string().describe("account_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/${args.account_id}`, undefined, undefined);
    },
  },
  {
    name: "github_users_list",
    description: "List users",
    inputSchema: z.object({
      since: z.number().optional().describe("A user ID. Only return users with an ID greater than this ID."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users`, undefined, { since: args.since, per_page: args.per_page });
    },
  },
  {
    name: "github_users_get_by_username",
    description: "Get a user",
    inputSchema: z.object({
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_users_list_attestations_bulk",
    description: "List attestations by bulk subject digests",
    inputSchema: z.object({
      username: z.string().describe("username"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      before: z.string().optional().describe("A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      after: z.string().optional().describe("A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/users/${args.username}/attestations/bulk-list`, args.body, { per_page: args.per_page, before: args.before, after: args.after });
    },
  },
  {
    name: "github_users_delete_attestations_bulk",
    description: "Delete attestations in bulk",
    inputSchema: z.object({
      username: z.string().describe("username"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/users/${args.username}/attestations/delete-request`, args.body, undefined);
    },
  },
  {
    name: "github_users_delete_attestations_by_subject_digest",
    description: "Delete attestations by subject digest",
    inputSchema: z.object({
      username: z.string().describe("username"),
      subject_digest: z.string().describe("subject_digest")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/users/${args.username}/attestations/digest/${args.subject_digest}`, undefined, undefined);
    },
  },
  {
    name: "github_users_delete_attestations_by_id",
    description: "Delete attestations by ID",
    inputSchema: z.object({
      username: z.string().describe("username"),
      attestation_id: z.string().describe("attestation_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/users/${args.username}/attestations/${args.attestation_id}`, undefined, undefined);
    },
  },
  {
    name: "github_users_list_attestations",
    description: "List attestations",
    inputSchema: z.object({
      username: z.string().describe("username"),
      subject_digest: z.string().describe("subject_digest"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      before: z.string().optional().describe("A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      after: z.string().optional().describe("A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      predicate_type: z.string().optional().describe("Optional filter for fetching attestations with a given predicate type. This option accepts `provenance`, `sbom`, `release`, or freeform text for custom predicate types.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/attestations/${args.subject_digest}`, undefined, { per_page: args.per_page, before: args.before, after: args.after, predicate_type: args.predicate_type });
    },
  },
  {
    name: "github_users_list_followers_for_user",
    description: "List followers of a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/followers`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_list_following_for_user",
    description: "List the people a user follows",
    inputSchema: z.object({
      username: z.string().describe("username"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/following`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_check_following_for_user",
    description: "Check if a user follows another user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      target_user: z.string().describe("target_user")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/following/${args.target_user}`, undefined, undefined);
    },
  },
  {
    name: "github_users_list_gpg_keys_for_user",
    description: "List GPG keys for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/gpg_keys`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_get_context_for_user",
    description: "Get contextual information for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      subject_type: z.enum(["organization", "repository", "issue", "pull_request"]).optional().describe("Identifies which additional information you'd like to receive about the person's hovercard. Can be `organization`, `repository`, `issue`, `pull_request`. **Required** when using `subject_id`."),
      subject_id: z.string().optional().describe("Uses the ID for the `subject_type` you specified. **Required** when using `subject_type`.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/hovercard`, undefined, { subject_type: args.subject_type, subject_id: args.subject_id });
    },
  },
  {
    name: "github_users_list_public_keys_for_user",
    description: "List public keys for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/keys`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_list_social_accounts_for_user",
    description: "List social accounts for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/social_accounts`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_users_list_ssh_signing_keys_for_user",
    description: "List SSH signing keys for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/ssh_signing_keys`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
];
