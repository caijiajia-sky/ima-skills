// AUTO-GENERATED by scripts/generate.ts — DO NOT EDIT
import { z } from "zod";
import { githubRequest } from "../client.js";

export const privateRegistriesTools = [
  {
    name: "github_private_registries_list_org_private_registries",
    description: "List private registries for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/private-registries`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_private_registries_create_org_private_registry",
    description: "Create a private registry for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/orgs/${args.org}/private-registries`, args.body, undefined);
    },
  },
  {
    name: "github_private_registries_get_org_public_key",
    description: "Get private registries public key for an organization",
    inputSchema: z.object({
      org: z.string().describe("org")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/private-registries/public-key`, undefined, undefined);
    },
  },
  {
    name: "github_private_registries_get_org_private_registry",
    description: "Get a private registry for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      secret_name: z.string().describe("secret_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/private-registries/${args.secret_name}`, undefined, undefined);
    },
  },
  {
    name: "github_private_registries_update_org_private_registry",
    description: "Update a private registry for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      secret_name: z.string().describe("secret_name"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/orgs/${args.org}/private-registries/${args.secret_name}`, args.body, undefined);
    },
  },
  {
    name: "github_private_registries_delete_org_private_registry",
    description: "Delete a private registry for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      secret_name: z.string().describe("secret_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/orgs/${args.org}/private-registries/${args.secret_name}`, undefined, undefined);
    },
  },
];
