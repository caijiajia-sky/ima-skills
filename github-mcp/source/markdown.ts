// AUTO-GENERATED by scripts/generate.ts — DO NOT EDIT
import { z } from "zod";
import { githubRequest } from "../client.js";

export const markdownTools = [
  {
    name: "github_markdown_render",
    description: "Render a Markdown document",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/markdown`, args.body, undefined);
    },
  },
  {
    name: "github_markdown_render_raw",
    description: "Render a Markdown document in raw mode",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/markdown/raw`, args.body, undefined);
    },
  },
];
