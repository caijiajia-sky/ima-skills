// AUTO-GENERATED by scripts/generate.ts — DO NOT EDIT
import { z } from "zod";
import { githubRequest } from "../client.js";

export const reposTools = [
  {
    name: "github_repos_list_for_org",
    description: "List organization repositories",
    inputSchema: z.object({
      org: z.string().describe("org"),
      type: z.enum(["all", "public", "private", "forks", "sources", "member"]).optional().describe("Specifies the types of repositories you want returned."),
      sort: z.enum(["created", "updated", "pushed", "full_name"]).optional().describe("The property to sort the results by."),
      direction: z.enum(["asc", "desc"]).optional().describe("The order to sort by. Default: `asc` when using `full_name`, otherwise `desc`."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/repos`, undefined, { type: args.type, sort: args.sort, direction: args.direction, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_create_in_org",
    description: "Create an organization repository",
    inputSchema: z.object({
      org: z.string().describe("org"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/orgs/${args.org}/repos`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_org_rulesets",
    description: "Get all organization repository rulesets",
    inputSchema: z.object({
      org: z.string().describe("org"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      targets: z.string().optional().describe("A comma-separated list of rule targets to filter by. If provided, only rulesets that apply to the specified targets will be returned. For example, `branch,tag,push`.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/rulesets`, undefined, { per_page: args.per_page, page: args.page, targets: args.targets });
    },
  },
  {
    name: "github_repos_create_org_ruleset",
    description: "Create an organization repository ruleset",
    inputSchema: z.object({
      org: z.string().describe("org"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/orgs/${args.org}/rulesets`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_org_rule_suites",
    description: "List organization rule suites",
    inputSchema: z.object({
      org: z.string().describe("org"),
      ref: z.string().optional().describe("The name of the ref. Cannot contain wildcard characters. Optionally prefix with `refs/heads/` to limit to branches or `refs/tags/` to limit to tags. Omit the prefix to search across all refs. When specified, only rule evaluations triggered for this ref will be returned."),
      repository_name: z.string().optional().describe("The name of the repository to filter on."),
      time_period: z.enum(["hour", "day", "week", "month"]).optional().describe("The time period to filter by.  For example, `day` will filter for rule suites that occurred in the past 24 hours, and `week` will filter for rule suites that occurred in the past 7 days (168 hours)."),
      actor_name: z.string().optional().describe("The handle for the GitHub user account to filter on. When specified, only rule evaluations triggered by this actor will be returned."),
      rule_suite_result: z.enum(["pass", "fail", "bypass", "all"]).optional().describe("The rule suite results to filter on. When specified, only suites with this result will be returned."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/rulesets/rule-suites`, undefined, { ref: args.ref, repository_name: args.repository_name, time_period: args.time_period, actor_name: args.actor_name, rule_suite_result: args.rule_suite_result, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_org_rule_suite",
    description: "Get an organization rule suite",
    inputSchema: z.object({
      org: z.string().describe("org"),
      rule_suite_id: z.string().describe("rule_suite_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/rulesets/rule-suites/${args.rule_suite_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_org_ruleset",
    description: "Get an organization repository ruleset",
    inputSchema: z.object({
      org: z.string().describe("org"),
      ruleset_id: z.string().describe("ruleset_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/rulesets/${args.ruleset_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_org_ruleset",
    description: "Update an organization repository ruleset",
    inputSchema: z.object({
      org: z.string().describe("org"),
      ruleset_id: z.string().describe("ruleset_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/orgs/${args.org}/rulesets/${args.ruleset_id}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_org_ruleset",
    description: "Delete an organization repository ruleset",
    inputSchema: z.object({
      org: z.string().describe("org"),
      ruleset_id: z.string().describe("ruleset_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/orgs/${args.org}/rulesets/${args.ruleset_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get",
    description: "Get a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update",
    description: "Update a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete",
    description: "Delete a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_activities",
    description: "List repository activities",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      direction: z.enum(["asc", "desc"]).optional().describe("The direction to sort the results by."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      before: z.string().optional().describe("A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      after: z.string().optional().describe("A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      ref: z.string().optional().describe("The Git reference for the activities you want to list.  The `ref` for a branch can be formatted either as `refs/heads/BRANCH_NAME` or `BRANCH_NAME`, where `BRANCH_NAME` is the name of your branch."),
      actor: z.string().optional().describe("The GitHub username to use to filter by the actor who performed the activity."),
      time_period: z.enum(["day", "week", "month", "quarter", "year"]).optional().describe("The time period to filter by.  For example, `day` will filter for activity that occurred in the past 24 hours, and `week` will filter for activity that occurred in the past 7 days (168 hours)."),
      activity_type: z.enum(["push", "force_push", "branch_creation", "branch_deletion", "pr_merge", "merge_queue_merge"]).optional().describe("The activity type to filter by.  For example, you can choose to filter by \"force_push\", to see all force pushes to the repository.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/activity`, undefined, { direction: args.direction, per_page: args.per_page, before: args.before, after: args.after, ref: args.ref, actor: args.actor, time_period: args.time_period, activity_type: args.activity_type });
    },
  },
  {
    name: "github_repos_create_attestation",
    description: "Create an attestation",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/attestations`, args.body, undefined);
    },
  },
  {
    name: "github_repos_list_attestations",
    description: "List attestations",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      subject_digest: z.string().describe("subject_digest"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      before: z.string().optional().describe("A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results before this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      after: z.string().optional().describe("A cursor, as given in the [Link header](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api#using-link-headers). If specified, the query only searches for results after this cursor. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      predicate_type: z.string().optional().describe("Optional filter for fetching attestations with a given predicate type. This option accepts `provenance`, `sbom`, `release`, or freeform text for custom predicate types.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/attestations/${args.subject_digest}`, undefined, { per_page: args.per_page, before: args.before, after: args.after, predicate_type: args.predicate_type });
    },
  },
  {
    name: "github_repos_list_autolinks",
    description: "Get all autolinks of a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/autolinks`, undefined, undefined);
    },
  },
  {
    name: "github_repos_create_autolink",
    description: "Create an autolink reference for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/autolinks`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_autolink",
    description: "Get an autolink reference of a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      autolink_id: z.string().describe("autolink_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/autolinks/${args.autolink_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_delete_autolink",
    description: "Delete an autolink reference from a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      autolink_id: z.string().describe("autolink_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/autolinks/${args.autolink_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_check_automated_security_fixes",
    description: "Check if Dependabot security updates are enabled for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/automated-security-fixes`, undefined, undefined);
    },
  },
  {
    name: "github_repos_enable_automated_security_fixes",
    description: "Enable Dependabot security updates",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/automated-security-fixes`, undefined, undefined);
    },
  },
  {
    name: "github_repos_disable_automated_security_fixes",
    description: "Disable Dependabot security updates",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/automated-security-fixes`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_branches",
    description: "List branches",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      protected: z.boolean().optional().describe("Setting to `true` returns only branches protected by branch protections or rulesets. When set to `false`, only unprotected branches are returned. Omitting this parameter returns all branches."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches`, undefined, { protected: args.protected, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_branch",
    description: "Get a branch",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_branch_protection",
    description: "Get branch protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_branch_protection",
    description: "Update branch protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_branch_protection",
    description: "Delete branch protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_admin_branch_protection",
    description: "Get admin branch protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/enforce_admins`, undefined, undefined);
    },
  },
  {
    name: "github_repos_set_admin_branch_protection",
    description: "Set admin branch protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/enforce_admins`, undefined, undefined);
    },
  },
  {
    name: "github_repos_delete_admin_branch_protection",
    description: "Delete admin branch protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/enforce_admins`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_pull_request_review_protection",
    description: "Get pull request review protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_pull_request_reviews`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_pull_request_review_protection",
    description: "Update pull request review protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_pull_request_reviews`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_pull_request_review_protection",
    description: "Delete pull request review protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_pull_request_reviews`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_commit_signature_protection",
    description: "Get commit signature protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_signatures`, undefined, undefined);
    },
  },
  {
    name: "github_repos_create_commit_signature_protection",
    description: "Create commit signature protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_signatures`, undefined, undefined);
    },
  },
  {
    name: "github_repos_delete_commit_signature_protection",
    description: "Delete commit signature protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_signatures`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_status_checks_protection",
    description: "Get status checks protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_status_checks`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_status_check_protection",
    description: "Update status check protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_status_checks`, args.body, undefined);
    },
  },
  {
    name: "github_repos_remove_status_check_protection",
    description: "Remove status check protection",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_status_checks`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_all_status_check_contexts",
    description: "Get all status check contexts",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_status_checks/contexts`, undefined, undefined);
    },
  },
  {
    name: "github_repos_add_status_check_contexts",
    description: "Add status check contexts",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_status_checks/contexts`, args.body, undefined);
    },
  },
  {
    name: "github_repos_set_status_check_contexts",
    description: "Set status check contexts",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_status_checks/contexts`, args.body, undefined);
    },
  },
  {
    name: "github_repos_remove_status_check_contexts",
    description: "Remove status check contexts",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/required_status_checks/contexts`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_access_restrictions",
    description: "Get access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions`, undefined, undefined);
    },
  },
  {
    name: "github_repos_delete_access_restrictions",
    description: "Delete access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_apps_with_access_to_protected_branch",
    description: "Get apps with access to the protected branch",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/apps`, undefined, undefined);
    },
  },
  {
    name: "github_repos_add_app_access_restrictions",
    description: "Add app access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/apps`, args.body, undefined);
    },
  },
  {
    name: "github_repos_set_app_access_restrictions",
    description: "Set app access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/apps`, args.body, undefined);
    },
  },
  {
    name: "github_repos_remove_app_access_restrictions",
    description: "Remove app access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/apps`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_teams_with_access_to_protected_branch",
    description: "Get teams with access to the protected branch",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/teams`, undefined, undefined);
    },
  },
  {
    name: "github_repos_add_team_access_restrictions",
    description: "Add team access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/teams`, args.body, undefined);
    },
  },
  {
    name: "github_repos_set_team_access_restrictions",
    description: "Set team access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/teams`, args.body, undefined);
    },
  },
  {
    name: "github_repos_remove_team_access_restrictions",
    description: "Remove team access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/teams`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_users_with_access_to_protected_branch",
    description: "Get users with access to the protected branch",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/users`, undefined, undefined);
    },
  },
  {
    name: "github_repos_add_user_access_restrictions",
    description: "Add user access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/users`, args.body, undefined);
    },
  },
  {
    name: "github_repos_set_user_access_restrictions",
    description: "Set user access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/users`, args.body, undefined);
    },
  },
  {
    name: "github_repos_remove_user_access_restrictions",
    description: "Remove user access restrictions",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/protection/restrictions/users`, undefined, undefined);
    },
  },
  {
    name: "github_repos_rename_branch",
    description: "Rename a branch",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/branches/${args.branch}/rename`, args.body, undefined);
    },
  },
  {
    name: "github_repos_codeowners_errors",
    description: "List CODEOWNERS errors",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ref: z.string().optional().describe("A branch, tag or commit name used to determine which version of the CODEOWNERS file to use. Default: the repository's default branch (e.g. `main`)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/codeowners/errors`, undefined, { ref: args.ref });
    },
  },
  {
    name: "github_repos_list_collaborators",
    description: "List repository collaborators",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      affiliation: z.enum(["outside", "direct", "all"]).optional().describe("Filter collaborators returned by their affiliation. `outside` means all outside collaborators of an organization-owned repository. `direct` means all collaborators with permissions to an organization-owned repository, regardless of organization membership status. `all` means all collaborators the authenticated user can see."),
      permission: z.enum(["pull", "triage", "push", "maintain", "admin"]).optional().describe("Filter collaborators by the permissions they have on the repository. If not specified, all collaborators will be returned."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/collaborators`, undefined, { affiliation: args.affiliation, permission: args.permission, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_check_collaborator",
    description: "Check if a user is a repository collaborator",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/collaborators/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_add_collaborator",
    description: "Add a repository collaborator",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      username: z.string().describe("username"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/collaborators/${args.username}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_remove_collaborator",
    description: "Remove a repository collaborator",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/collaborators/${args.username}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_collaborator_permission_level",
    description: "Get repository permissions for a user",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/collaborators/${args.username}/permission`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_commit_comments_for_repo",
    description: "List commit comments for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/comments`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_commit_comment",
    description: "Get a commit comment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      comment_id: z.string().describe("comment_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/comments/${args.comment_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_commit_comment",
    description: "Update a commit comment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      comment_id: z.string().describe("comment_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}/comments/${args.comment_id}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_commit_comment",
    description: "Delete a commit comment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      comment_id: z.string().describe("comment_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/comments/${args.comment_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_commits",
    description: "List commits",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      sha: z.string().optional().describe("SHA or branch to start listing commits from. Default: the repository’s default branch (usually `main`)."),
      path: z.string().optional().describe("Only commits containing this file path will be returned."),
      author: z.string().optional().describe("GitHub username or email address to use to filter by commit author."),
      committer: z.string().optional().describe("GitHub username or email address to use to filter by commit committer."),
      since: z.string().optional().describe("Only show results that were last updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Due to limitations of Git, timestamps must be between 1970-01-01 and 2099-12-31 (inclusive) or unexpected results may be returned."),
      until: z.string().optional().describe("Only commits before this date will be returned. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Due to limitations of Git, timestamps must be between 1970-01-01 and 2099-12-31 (inclusive) or unexpected results may be returned."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/commits`, undefined, { sha: args.sha, path: args.path, author: args.author, committer: args.committer, since: args.since, until: args.until, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_list_branches_for_head_commit",
    description: "List branches for HEAD commit",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      commit_sha: z.string().describe("commit_sha")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/commits/${args.commit_sha}/branches-where-head`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_comments_for_commit",
    description: "List commit comments",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      commit_sha: z.string().describe("commit_sha"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/commits/${args.commit_sha}/comments`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_create_commit_comment",
    description: "Create a commit comment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      commit_sha: z.string().describe("commit_sha"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/commits/${args.commit_sha}/comments`, args.body, undefined);
    },
  },
  {
    name: "github_repos_list_pull_requests_associated_with_commit",
    description: "List pull requests associated with a commit",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      commit_sha: z.string().describe("commit_sha"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/commits/${args.commit_sha}/pulls`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_commit",
    description: "Get a commit",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ref: z.string().describe("ref"),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/commits/${args.ref}`, undefined, { page: args.page, per_page: args.per_page });
    },
  },
  {
    name: "github_repos_get_combined_status_for_ref",
    description: "Get the combined status for a specific reference",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ref: z.string().describe("ref"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/commits/${args.ref}/status`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_list_commit_statuses_for_ref",
    description: "List commit statuses for a reference",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ref: z.string().describe("ref"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/commits/${args.ref}/statuses`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_community_profile_metrics",
    description: "Get community profile metrics",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/community/profile`, undefined, undefined);
    },
  },
  {
    name: "github_repos_compare_commits",
    description: "Compare two commits",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      basehead: z.string().describe("basehead"),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/compare/${args.basehead}`, undefined, { page: args.page, per_page: args.per_page });
    },
  },
  {
    name: "github_repos_get_content",
    description: "Get repository content",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      path: z.string().describe("path"),
      ref: z.string().optional().describe("The name of the commit/branch/tag. Default: the repository’s default branch.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/contents/${args.path}`, undefined, { ref: args.ref });
    },
  },
  {
    name: "github_repos_create_or_update_file_contents",
    description: "Create or update file contents",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      path: z.string().describe("path"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/contents/${args.path}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_file",
    description: "Delete a file",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      path: z.string().describe("path")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/contents/${args.path}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_contributors",
    description: "List repository contributors",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      anon: z.string().optional().describe("Set to `1` or `true` to include anonymous contributors in results."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/contributors`, undefined, { anon: args.anon, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_list_deployments",
    description: "List deployments",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      sha: z.string().optional().describe("The SHA recorded at creation time."),
      ref: z.string().optional().describe("The name of the ref. This can be a branch, tag, or SHA."),
      task: z.string().optional().describe("The name of the task for the deployment (e.g., `deploy` or `deploy:migrations`)."),
      environment: z.string().optional().describe("The name of the environment that was deployed to (e.g., `staging` or `production`)."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/deployments`, undefined, { sha: args.sha, ref: args.ref, task: args.task, environment: args.environment, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_create_deployment",
    description: "Create a deployment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/deployments`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_deployment",
    description: "Get a deployment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      deployment_id: z.string().describe("deployment_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/deployments/${args.deployment_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_delete_deployment",
    description: "Delete a deployment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      deployment_id: z.string().describe("deployment_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/deployments/${args.deployment_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_deployment_statuses",
    description: "List deployment statuses",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      deployment_id: z.string().describe("deployment_id"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/deployments/${args.deployment_id}/statuses`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_create_deployment_status",
    description: "Create a deployment status",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      deployment_id: z.string().describe("deployment_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/deployments/${args.deployment_id}/statuses`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_deployment_status",
    description: "Get a deployment status",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      deployment_id: z.string().describe("deployment_id"),
      status_id: z.string().describe("status_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/deployments/${args.deployment_id}/statuses/${args.status_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_create_dispatch_event",
    description: "Create a repository dispatch event",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/dispatches`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_all_environments",
    description: "List environments",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/environments`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_environment",
    description: "Get an environment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_create_or_update_environment",
    description: "Create or update an environment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_an_environment",
    description: "Delete an environment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_deployment_branch_policies",
    description: "List deployment branch policies",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment-branch-policies`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_create_deployment_branch_policy",
    description: "Create a deployment branch policy",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment-branch-policies`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_deployment_branch_policy",
    description: "Get a deployment branch policy",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      branch_policy_id: z.string().describe("branch_policy_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment-branch-policies/${args.branch_policy_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_deployment_branch_policy",
    description: "Update a deployment branch policy",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      branch_policy_id: z.string().describe("branch_policy_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment-branch-policies/${args.branch_policy_id}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_deployment_branch_policy",
    description: "Delete a deployment branch policy",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      branch_policy_id: z.string().describe("branch_policy_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment-branch-policies/${args.branch_policy_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_all_deployment_protection_rules",
    description: "Get all deployment protection rules for an environment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment_protection_rules`, undefined, undefined);
    },
  },
  {
    name: "github_repos_create_deployment_protection_rule",
    description: "Create a custom deployment protection rule on an environment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment_protection_rules`, args.body, undefined);
    },
  },
  {
    name: "github_repos_list_custom_deployment_rule_integrations",
    description: "List custom deployment rule integrations available for an environment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment_protection_rules/apps`, undefined, { page: args.page, per_page: args.per_page });
    },
  },
  {
    name: "github_repos_get_custom_deployment_protection_rule",
    description: "Get a custom deployment protection rule",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      protection_rule_id: z.string().describe("protection_rule_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment_protection_rules/${args.protection_rule_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_disable_deployment_protection_rule",
    description: "Disable a custom protection rule for an environment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      environment_name: z.string().describe("environment_name"),
      protection_rule_id: z.string().describe("protection_rule_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/environments/${args.environment_name}/deployment_protection_rules/${args.protection_rule_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_forks",
    description: "List forks",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      sort: z.enum(["newest", "oldest", "stargazers", "watchers"]).optional().describe("The sort order. `stargazers` will sort by star count."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/forks`, undefined, { sort: args.sort, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_create_fork",
    description: "Create a fork",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/forks`, args.body, undefined);
    },
  },
  {
    name: "github_repos_list_webhooks",
    description: "List repository webhooks",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/hooks`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_create_webhook",
    description: "Create a repository webhook",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/hooks`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_webhook",
    description: "Get a repository webhook",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_webhook",
    description: "Update a repository webhook",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_webhook",
    description: "Delete a repository webhook",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_webhook_config_for_repo",
    description: "Get a webhook configuration for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}/config`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_webhook_config_for_repo",
    description: "Update a webhook configuration for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}/config`, args.body, undefined);
    },
  },
  {
    name: "github_repos_list_webhook_deliveries",
    description: "List deliveries for a repository webhook",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      cursor: z.string().optional().describe("Used for pagination: the starting delivery from which the page of deliveries is fetched. Refer to the `link` header for the next and previous page cursors."),
      status: z.enum(["success", "failure"]).optional().describe("Returns webhook deliveries filtered by delivery outcome classification based on `status_code` range. A `status` of `success` returns deliveries with a `status_code` in the 200-399 range (inclusive). A `status` of `failure` returns deliveries with a `status_code` in the 400-599 range (inclusive).")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}/deliveries`, undefined, { per_page: args.per_page, cursor: args.cursor, status: args.status });
    },
  },
  {
    name: "github_repos_get_webhook_delivery",
    description: "Get a delivery for a repository webhook",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id"),
      delivery_id: z.string().describe("delivery_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}/deliveries/${args.delivery_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_redeliver_webhook_delivery",
    description: "Redeliver a delivery for a repository webhook",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id"),
      delivery_id: z.string().describe("delivery_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}/deliveries/${args.delivery_id}/attempts`, undefined, undefined);
    },
  },
  {
    name: "github_repos_ping_webhook",
    description: "Ping a repository webhook",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}/pings`, undefined, undefined);
    },
  },
  {
    name: "github_repos_test_push_webhook",
    description: "Test the push repository webhook",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      hook_id: z.string().describe("hook_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/hooks/${args.hook_id}/tests`, undefined, undefined);
    },
  },
  {
    name: "github_repos_check_immutable_releases",
    description: "Check if immutable releases are enabled for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/immutable-releases`, undefined, undefined);
    },
  },
  {
    name: "github_repos_enable_immutable_releases",
    description: "Enable immutable releases",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/immutable-releases`, undefined, undefined);
    },
  },
  {
    name: "github_repos_disable_immutable_releases",
    description: "Disable immutable releases",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/immutable-releases`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_invitations",
    description: "List repository invitations",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/invitations`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_update_invitation",
    description: "Update a repository invitation",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      invitation_id: z.string().describe("invitation_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}/invitations/${args.invitation_id}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_invitation",
    description: "Delete a repository invitation",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      invitation_id: z.string().describe("invitation_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/invitations/${args.invitation_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_deploy_keys",
    description: "List deploy keys",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/keys`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_create_deploy_key",
    description: "Create a deploy key",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/keys`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_deploy_key",
    description: "Get a deploy key",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      key_id: z.string().describe("key_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/keys/${args.key_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_delete_deploy_key",
    description: "Delete a deploy key",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      key_id: z.string().describe("key_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/keys/${args.key_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_languages",
    description: "List repository languages",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/languages`, undefined, undefined);
    },
  },
  {
    name: "github_repos_merge_upstream",
    description: "Sync a fork branch with the upstream repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/merge-upstream`, args.body, undefined);
    },
  },
  {
    name: "github_repos_merge",
    description: "Merge a branch",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/merges`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_pages",
    description: "Get a GitHub Pages site",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/pages`, undefined, undefined);
    },
  },
  {
    name: "github_repos_create_pages_site",
    description: "Create a GitHub Pages site",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/pages`, args.body, undefined);
    },
  },
  {
    name: "github_repos_update_information_about_pages_site",
    description: "Update information about a GitHub Pages site",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/pages`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_pages_site",
    description: "Delete a GitHub Pages site",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/pages`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_pages_builds",
    description: "List GitHub Pages builds",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/pages/builds`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_request_pages_build",
    description: "Request a GitHub Pages build",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/pages/builds`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_latest_pages_build",
    description: "Get latest Pages build",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/pages/builds/latest`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_pages_build",
    description: "Get GitHub Pages build",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      build_id: z.string().describe("build_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/pages/builds/${args.build_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_create_pages_deployment",
    description: "Create a GitHub Pages deployment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/pages/deployments`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_pages_deployment",
    description: "Get the status of a GitHub Pages deployment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      pages_deployment_id: z.string().describe("pages_deployment_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/pages/deployments/${args.pages_deployment_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_cancel_pages_deployment",
    description: "Cancel a GitHub Pages deployment",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      pages_deployment_id: z.string().describe("pages_deployment_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/pages/deployments/${args.pages_deployment_id}/cancel`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_pages_health_check",
    description: "Get a DNS health check for GitHub Pages",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/pages/health`, undefined, undefined);
    },
  },
  {
    name: "github_repos_check_private_vulnerability_reporting",
    description: "Check if private vulnerability reporting is enabled for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/private-vulnerability-reporting`, undefined, undefined);
    },
  },
  {
    name: "github_repos_enable_private_vulnerability_reporting",
    description: "Enable private vulnerability reporting for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/private-vulnerability-reporting`, undefined, undefined);
    },
  },
  {
    name: "github_repos_disable_private_vulnerability_reporting",
    description: "Disable private vulnerability reporting for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/private-vulnerability-reporting`, undefined, undefined);
    },
  },
  {
    name: "github_repos_custom_properties_for_repos_get_repository_values",
    description: "Get all custom property values for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/properties/values`, undefined, undefined);
    },
  },
  {
    name: "github_repos_custom_properties_for_repos_create_or_update_repository_values",
    description: "Create or update custom property values for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}/properties/values`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_readme",
    description: "Get a repository README",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ref: z.string().optional().describe("The name of the commit/branch/tag. Default: the repository’s default branch.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/readme`, undefined, { ref: args.ref });
    },
  },
  {
    name: "github_repos_get_readme_in_directory",
    description: "Get a repository README for a directory",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      dir: z.string().describe("dir"),
      ref: z.string().optional().describe("The name of the commit/branch/tag. Default: the repository’s default branch.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/readme/${args.dir}`, undefined, { ref: args.ref });
    },
  },
  {
    name: "github_repos_list_releases",
    description: "List releases",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/releases`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_create_release",
    description: "Create a release",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/releases`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_release_asset",
    description: "Get a release asset",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      asset_id: z.string().describe("asset_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/releases/assets/${args.asset_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_release_asset",
    description: "Update a release asset",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      asset_id: z.string().describe("asset_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}/releases/assets/${args.asset_id}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_release_asset",
    description: "Delete a release asset",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      asset_id: z.string().describe("asset_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/releases/assets/${args.asset_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_generate_release_notes",
    description: "Generate release notes content for a release",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/releases/generate-notes`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_latest_release",
    description: "Get the latest release",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/releases/latest`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_release_by_tag",
    description: "Get a release by tag name",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      tag: z.string().describe("tag")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/releases/tags/${args.tag}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_release",
    description: "Get a release",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      release_id: z.string().describe("release_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/releases/${args.release_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_update_release",
    description: "Update a release",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      release_id: z.string().describe("release_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/repos/${args.owner}/${args.repo}/releases/${args.release_id}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_release",
    description: "Delete a release",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      release_id: z.string().describe("release_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/releases/${args.release_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_release_assets",
    description: "List release assets",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      release_id: z.string().describe("release_id"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/releases/${args.release_id}/assets`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_upload_release_asset",
    description: "Upload a release asset",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      release_id: z.string().describe("release_id"),
      name: z.string().describe("name"),
      label: z.string().optional().describe("label"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/releases/${args.release_id}/assets`, args.body, { name: args.name, label: args.label });
    },
  },
  {
    name: "github_repos_get_branch_rules",
    description: "Get rules for a branch",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      branch: z.string().describe("branch"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/rules/branches/${args.branch}`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_repo_rulesets",
    description: "Get all repository rulesets",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      includes_parents: z.boolean().optional().describe("Include rulesets configured at higher levels that apply to this repository"),
      targets: z.string().optional().describe("A comma-separated list of rule targets to filter by. If provided, only rulesets that apply to the specified targets will be returned. For example, `branch,tag,push`.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/rulesets`, undefined, { per_page: args.per_page, page: args.page, includes_parents: args.includes_parents, targets: args.targets });
    },
  },
  {
    name: "github_repos_create_repo_ruleset",
    description: "Create a repository ruleset",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/rulesets`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_repo_rule_suites",
    description: "List repository rule suites",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ref: z.string().optional().describe("The name of the ref. Cannot contain wildcard characters. Optionally prefix with `refs/heads/` to limit to branches or `refs/tags/` to limit to tags. Omit the prefix to search across all refs. When specified, only rule evaluations triggered for this ref will be returned."),
      time_period: z.enum(["hour", "day", "week", "month"]).optional().describe("The time period to filter by.  For example, `day` will filter for rule suites that occurred in the past 24 hours, and `week` will filter for rule suites that occurred in the past 7 days (168 hours)."),
      actor_name: z.string().optional().describe("The handle for the GitHub user account to filter on. When specified, only rule evaluations triggered by this actor will be returned."),
      rule_suite_result: z.enum(["pass", "fail", "bypass", "all"]).optional().describe("The rule suite results to filter on. When specified, only suites with this result will be returned."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/rulesets/rule-suites`, undefined, { ref: args.ref, time_period: args.time_period, actor_name: args.actor_name, rule_suite_result: args.rule_suite_result, per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_repo_rule_suite",
    description: "Get a repository rule suite",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      rule_suite_id: z.string().describe("rule_suite_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/rulesets/rule-suites/${args.rule_suite_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_repo_ruleset",
    description: "Get a repository ruleset",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ruleset_id: z.string().describe("ruleset_id"),
      includes_parents: z.boolean().optional().describe("Include rulesets configured at higher levels that apply to this repository")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/rulesets/${args.ruleset_id}`, undefined, { includes_parents: args.includes_parents });
    },
  },
  {
    name: "github_repos_update_repo_ruleset",
    description: "Update a repository ruleset",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ruleset_id: z.string().describe("ruleset_id"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/rulesets/${args.ruleset_id}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_delete_repo_ruleset",
    description: "Delete a repository ruleset",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ruleset_id: z.string().describe("ruleset_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/rulesets/${args.ruleset_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_repo_ruleset_history",
    description: "Get repository ruleset history",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ruleset_id: z.string().describe("ruleset_id"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/rulesets/${args.ruleset_id}/history`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_repo_ruleset_version",
    description: "Get repository ruleset version",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ruleset_id: z.string().describe("ruleset_id"),
      version_id: z.string().describe("version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/rulesets/${args.ruleset_id}/history/${args.version_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_code_frequency_stats",
    description: "Get the weekly commit activity",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/stats/code_frequency`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_commit_activity_stats",
    description: "Get the last year of commit activity",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/stats/commit_activity`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_contributors_stats",
    description: "Get all contributor commit activity",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/stats/contributors`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_participation_stats",
    description: "Get the weekly commit count",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/stats/participation`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_punch_card_stats",
    description: "Get the hourly commit count for each day",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/stats/punch_card`, undefined, undefined);
    },
  },
  {
    name: "github_repos_create_commit_status",
    description: "Create a commit status",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      sha: z.string().describe("sha"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/statuses/${args.sha}`, args.body, undefined);
    },
  },
  {
    name: "github_repos_list_tags",
    description: "List repository tags",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/tags`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_download_tarball_archive",
    description: "Download a repository archive (tar)",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ref: z.string().describe("ref")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/tarball/${args.ref}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_teams",
    description: "List repository teams",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/teams`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_get_all_topics",
    description: "Get all repository topics",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/topics`, undefined, { page: args.page, per_page: args.per_page });
    },
  },
  {
    name: "github_repos_replace_all_topics",
    description: "Replace all repository topics",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/topics`, args.body, undefined);
    },
  },
  {
    name: "github_repos_get_clones",
    description: "Get repository clones",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per: z.enum(["day", "week"]).optional().describe("The time frame to display results for.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/traffic/clones`, undefined, { per: args.per });
    },
  },
  {
    name: "github_repos_get_top_paths",
    description: "Get top referral paths",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/traffic/popular/paths`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_top_referrers",
    description: "Get top referral sources",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/traffic/popular/referrers`, undefined, undefined);
    },
  },
  {
    name: "github_repos_get_views",
    description: "Get page views",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      per: z.enum(["day", "week"]).optional().describe("The time frame to display results for.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/traffic/views`, undefined, { per: args.per });
    },
  },
  {
    name: "github_repos_transfer",
    description: "Transfer a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.owner}/${args.repo}/transfer`, args.body, undefined);
    },
  },
  {
    name: "github_repos_check_vulnerability_alerts",
    description: "Check if vulnerability alerts are enabled for a repository",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/vulnerability-alerts`, undefined, undefined);
    },
  },
  {
    name: "github_repos_enable_vulnerability_alerts",
    description: "Enable vulnerability alerts",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PUT", `/repos/${args.owner}/${args.repo}/vulnerability-alerts`, undefined, undefined);
    },
  },
  {
    name: "github_repos_disable_vulnerability_alerts",
    description: "Disable vulnerability alerts",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/repos/${args.owner}/${args.repo}/vulnerability-alerts`, undefined, undefined);
    },
  },
  {
    name: "github_repos_download_zipball_archive",
    description: "Download a repository archive (zip)",
    inputSchema: z.object({
      owner: z.string().describe("owner"),
      repo: z.string().describe("repo"),
      ref: z.string().describe("ref")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repos/${args.owner}/${args.repo}/zipball/${args.ref}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_create_using_template",
    description: "Create a repository using a template",
    inputSchema: z.object({
      template_owner: z.string().describe("template_owner"),
      template_repo: z.string().describe("template_repo"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/repos/${args.template_owner}/${args.template_repo}/generate`, args.body, undefined);
    },
  },
  {
    name: "github_repos_list_public",
    description: "List public repositories",
    inputSchema: z.object({
      since: z.number().optional().describe("A repository ID. Only return repositories with an ID greater than this ID.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/repositories`, undefined, { since: args.since });
    },
  },
  {
    name: "github_repos_list_for_authenticated_user",
    description: "List repositories for the authenticated user",
    inputSchema: z.object({
      visibility: z.enum(["all", "public", "private"]).optional().describe("Limit results to repositories with the specified visibility."),
      affiliation: z.string().optional().describe("Comma-separated list of values. Can include:    * `owner`: Repositories that are owned by the authenticated user.    * `collaborator`: Repositories that the user has been added to as a collaborator.    * `organization_member`: Repositories that the user has access to through being a member of an organization. This includes every repository on every team that the user is on."),
      type: z.enum(["all", "owner", "public", "private", "member"]).optional().describe("Limit results to repositories of the specified type. Will cause a `422` error if used in the same request as **visibility** or **affiliation**."),
      sort: z.enum(["created", "updated", "pushed", "full_name"]).optional().describe("The property to sort the results by."),
      direction: z.enum(["asc", "desc"]).optional().describe("The order to sort by. Default: `asc` when using `full_name`, otherwise `desc`."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      since: z.string().optional().describe("Only show repositories updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`."),
      before: z.string().optional().describe("Only show repositories updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/repos`, undefined, { visibility: args.visibility, affiliation: args.affiliation, type: args.type, sort: args.sort, direction: args.direction, per_page: args.per_page, page: args.page, since: args.since, before: args.before });
    },
  },
  {
    name: "github_repos_create_for_authenticated_user",
    description: "Create a repository for the authenticated user",
    inputSchema: z.object({
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/user/repos`, args.body, undefined);
    },
  },
  {
    name: "github_repos_list_invitations_for_authenticated_user",
    description: "List repository invitations for the authenticated user",
    inputSchema: z.object({
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/repository_invitations`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_repos_accept_invitation_for_authenticated_user",
    description: "Accept a repository invitation",
    inputSchema: z.object({
      invitation_id: z.string().describe("invitation_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/user/repository_invitations/${args.invitation_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_decline_invitation_for_authenticated_user",
    description: "Decline a repository invitation",
    inputSchema: z.object({
      invitation_id: z.string().describe("invitation_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/repository_invitations/${args.invitation_id}`, undefined, undefined);
    },
  },
  {
    name: "github_repos_list_for_user",
    description: "List repositories for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      type: z.enum(["all", "owner", "member"]).optional().describe("Limit results to repositories of the specified type."),
      sort: z.enum(["created", "updated", "pushed", "full_name"]).optional().describe("The property to sort the results by."),
      direction: z.enum(["asc", "desc"]).optional().describe("The order to sort by. Default: `asc` when using `full_name`, otherwise `desc`."),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/repos`, undefined, { type: args.type, sort: args.sort, direction: args.direction, per_page: args.per_page, page: args.page });
    },
  },
];
