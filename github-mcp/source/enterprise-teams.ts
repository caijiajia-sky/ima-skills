// AUTO-GENERATED by scripts/generate.ts — DO NOT EDIT
import { z } from "zod";
import { githubRequest } from "../client.js";

export const enterpriseTeamsTools = [
  {
    name: "github_enterprise_teams_list",
    description: "List enterprise teams",
    inputSchema: z.object({
      enterprise: z.string().describe("enterprise"),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/enterprises/${args.enterprise}/teams`, undefined, { per_page: args.per_page, page: args.page });
    },
  },
  {
    name: "github_enterprise_teams_create",
    description: "Create an enterprise team",
    inputSchema: z.object({
      enterprise: z.string().describe("enterprise"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/enterprises/${args.enterprise}/teams`, args.body, undefined);
    },
  },
  {
    name: "github_enterprise_teams_get",
    description: "Get an enterprise team",
    inputSchema: z.object({
      enterprise: z.string().describe("enterprise"),
      team_slug: z.string().describe("team_slug")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/enterprises/${args.enterprise}/teams/${args.team_slug}`, undefined, undefined);
    },
  },
  {
    name: "github_enterprise_teams_update",
    description: "Update an enterprise team",
    inputSchema: z.object({
      enterprise: z.string().describe("enterprise"),
      team_slug: z.string().describe("team_slug"),
      body: z.record(z.string(), z.unknown()).optional().describe("Request body (JSON object)")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("PATCH", `/enterprises/${args.enterprise}/teams/${args.team_slug}`, args.body, undefined);
    },
  },
  {
    name: "github_enterprise_teams_delete",
    description: "Delete an enterprise team",
    inputSchema: z.object({
      enterprise: z.string().describe("enterprise"),
      team_slug: z.string().describe("team_slug")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/enterprises/${args.enterprise}/teams/${args.team_slug}`, undefined, undefined);
    },
  },
];
