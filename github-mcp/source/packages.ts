// AUTO-GENERATED by scripts/generate.ts — DO NOT EDIT
import { z } from "zod";
import { githubRequest } from "../client.js";

export const packagesTools = [
  {
    name: "github_packages_list_docker_migration_conflicting_packages_for_organization",
    description: "Get list of conflicting packages during Docker migration for organization",
    inputSchema: z.object({
      org: z.string().describe("org")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/docker/conflicts`, undefined, undefined);
    },
  },
  {
    name: "github_packages_list_packages_for_organization",
    description: "List packages for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      package_type: z.enum(["npm", "maven", "rubygems", "docker", "nuget", "container"]).describe("The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."),
      visibility: z.enum(["public", "private", "internal"]).optional().describe("The selected visibility of the packages.  This parameter is optional and only filters an existing result set.  The `internal` visibility is only supported for GitHub Packages registries that allow for granular permissions. For other ecosystems `internal` is synonymous with `private`. For the list of GitHub Packages registries that support granular permissions, see \"[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/packages`, undefined, { package_type: args.package_type, visibility: args.visibility, page: args.page, per_page: args.per_page });
    },
  },
  {
    name: "github_packages_get_package_for_organization",
    description: "Get a package for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/packages/${args.package_type}/${args.package_name}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_delete_package_for_org",
    description: "Delete a package for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/orgs/${args.org}/packages/${args.package_type}/${args.package_name}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_restore_package_for_org",
    description: "Restore a package for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      token: z.string().optional().describe("package token")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/orgs/${args.org}/packages/${args.package_type}/${args.package_name}/restore`, undefined, { token: args.token });
    },
  },
  {
    name: "github_packages_get_all_package_versions_for_package_owned_by_org",
    description: "List package versions for a package owned by an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      state: z.enum(["active", "deleted"]).optional().describe("The state of the package, either active or deleted.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/packages/${args.package_type}/${args.package_name}/versions`, undefined, { page: args.page, per_page: args.per_page, state: args.state });
    },
  },
  {
    name: "github_packages_get_package_version_for_organization",
    description: "Get a package version for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      package_version_id: z.string().describe("package_version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/orgs/${args.org}/packages/${args.package_type}/${args.package_name}/versions/${args.package_version_id}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_delete_package_version_for_org",
    description: "Delete package version for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      package_version_id: z.string().describe("package_version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/orgs/${args.org}/packages/${args.package_type}/${args.package_name}/versions/${args.package_version_id}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_restore_package_version_for_org",
    description: "Restore package version for an organization",
    inputSchema: z.object({
      org: z.string().describe("org"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      package_version_id: z.string().describe("package_version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/orgs/${args.org}/packages/${args.package_type}/${args.package_name}/versions/${args.package_version_id}/restore`, undefined, undefined);
    },
  },
  {
    name: "github_packages_list_docker_migration_conflicting_packages_for_authenticated_user",
    description: "Get list of conflicting packages during Docker migration for authenticated-user",
    inputSchema: z.object({}),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/docker/conflicts`, undefined, undefined);
    },
  },
  {
    name: "github_packages_list_packages_for_authenticated_user",
    description: "List packages for the authenticated user's namespace",
    inputSchema: z.object({
      package_type: z.enum(["npm", "maven", "rubygems", "docker", "nuget", "container"]).describe("The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."),
      visibility: z.enum(["public", "private", "internal"]).optional().describe("The selected visibility of the packages.  This parameter is optional and only filters an existing result set.  The `internal` visibility is only supported for GitHub Packages registries that allow for granular permissions. For other ecosystems `internal` is synonymous with `private`. For the list of GitHub Packages registries that support granular permissions, see \"[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/packages`, undefined, { package_type: args.package_type, visibility: args.visibility, page: args.page, per_page: args.per_page });
    },
  },
  {
    name: "github_packages_get_package_for_authenticated_user",
    description: "Get a package for the authenticated user",
    inputSchema: z.object({
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/packages/${args.package_type}/${args.package_name}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_delete_package_for_authenticated_user",
    description: "Delete a package for the authenticated user",
    inputSchema: z.object({
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/packages/${args.package_type}/${args.package_name}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_restore_package_for_authenticated_user",
    description: "Restore a package for the authenticated user",
    inputSchema: z.object({
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      token: z.string().optional().describe("package token")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/user/packages/${args.package_type}/${args.package_name}/restore`, undefined, { token: args.token });
    },
  },
  {
    name: "github_packages_get_all_package_versions_for_package_owned_by_authenticated_user",
    description: "List package versions for a package owned by the authenticated user",
    inputSchema: z.object({
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      state: z.enum(["active", "deleted"]).optional().describe("The state of the package, either active or deleted.")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/packages/${args.package_type}/${args.package_name}/versions`, undefined, { page: args.page, per_page: args.per_page, state: args.state });
    },
  },
  {
    name: "github_packages_get_package_version_for_authenticated_user",
    description: "Get a package version for the authenticated user",
    inputSchema: z.object({
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      package_version_id: z.string().describe("package_version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/user/packages/${args.package_type}/${args.package_name}/versions/${args.package_version_id}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_delete_package_version_for_authenticated_user",
    description: "Delete a package version for the authenticated user",
    inputSchema: z.object({
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      package_version_id: z.string().describe("package_version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/user/packages/${args.package_type}/${args.package_name}/versions/${args.package_version_id}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_restore_package_version_for_authenticated_user",
    description: "Restore a package version for the authenticated user",
    inputSchema: z.object({
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      package_version_id: z.string().describe("package_version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/user/packages/${args.package_type}/${args.package_name}/versions/${args.package_version_id}/restore`, undefined, undefined);
    },
  },
  {
    name: "github_packages_list_docker_migration_conflicting_packages_for_user",
    description: "Get list of conflicting packages during Docker migration for user",
    inputSchema: z.object({
      username: z.string().describe("username")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/docker/conflicts`, undefined, undefined);
    },
  },
  {
    name: "github_packages_list_packages_for_user",
    description: "List packages for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      package_type: z.enum(["npm", "maven", "rubygems", "docker", "nuget", "container"]).describe("The type of supported package. Packages in GitHub's Gradle registry have the type `maven`. Docker images pushed to GitHub's Container registry (`ghcr.io`) have the type `container`. You can use the type `docker` to find images that were pushed to GitHub's Docker registry (`docker.pkg.github.com`), even if these have now been migrated to the Container registry."),
      visibility: z.enum(["public", "private", "internal"]).optional().describe("The selected visibility of the packages.  This parameter is optional and only filters an existing result set.  The `internal` visibility is only supported for GitHub Packages registries that allow for granular permissions. For other ecosystems `internal` is synonymous with `private`. For the list of GitHub Packages registries that support granular permissions, see \"[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages).\""),
      page: z.number().optional().describe("The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""),
      per_page: z.number().optional().describe("The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/packages`, undefined, { package_type: args.package_type, visibility: args.visibility, page: args.page, per_page: args.per_page });
    },
  },
  {
    name: "github_packages_get_package_for_user",
    description: "Get a package for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/packages/${args.package_type}/${args.package_name}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_delete_package_for_user",
    description: "Delete a package for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/users/${args.username}/packages/${args.package_type}/${args.package_name}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_restore_package_for_user",
    description: "Restore a package for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      token: z.string().optional().describe("package token")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/users/${args.username}/packages/${args.package_type}/${args.package_name}/restore`, undefined, { token: args.token });
    },
  },
  {
    name: "github_packages_get_all_package_versions_for_package_owned_by_user",
    description: "List package versions for a package owned by a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/packages/${args.package_type}/${args.package_name}/versions`, undefined, undefined);
    },
  },
  {
    name: "github_packages_get_package_version_for_user",
    description: "Get a package version for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      package_version_id: z.string().describe("package_version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("GET", `/users/${args.username}/packages/${args.package_type}/${args.package_name}/versions/${args.package_version_id}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_delete_package_version_for_user",
    description: "Delete package version for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      package_version_id: z.string().describe("package_version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("DELETE", `/users/${args.username}/packages/${args.package_type}/${args.package_name}/versions/${args.package_version_id}`, undefined, undefined);
    },
  },
  {
    name: "github_packages_restore_package_version_for_user",
    description: "Restore package version for a user",
    inputSchema: z.object({
      username: z.string().describe("username"),
      package_type: z.string().describe("package_type"),
      package_name: z.string().describe("package_name"),
      package_version_id: z.string().describe("package_version_id")
    }),
    handler: async (args: Record<string, any>) => {
      return githubRequest("POST", `/users/${args.username}/packages/${args.package_type}/${args.package_name}/versions/${args.package_version_id}/restore`, undefined, undefined);
    },
  },
];
